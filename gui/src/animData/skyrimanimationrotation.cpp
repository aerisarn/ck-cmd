#include "skyrimanimationrotation.h"

SkyrimAnimationRotation::SkyrimAnimationRotation(qreal time, qreal c2, qreal c3, qreal c4, qreal c5)
    : localTime(time), x(c2), y(c3), z(c4), w(c5)
{
    //
}
